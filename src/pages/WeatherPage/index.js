import Header from "../../components/Header";
import { Link } from "react-router-dom";

const WeatherPage = () => {
  return (
    <div>
      <Header />
      Weather page
    </div>
  );
};

export default WeatherPage;
